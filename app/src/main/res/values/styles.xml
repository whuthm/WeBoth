<resources>

    <style name="ToolbarTitleTextAppearance" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/font_large</item>
    </style>

    <!--必须要继承自Widget.AppCompat.Toolbar.Button.Navigation，否则Icon位置显示错误-->
    <style name="ToolbarNavigationButtonStyle" parent="Widget.AppCompat.Toolbar.Button.Navigation">
        <item name="android:paddingLeft">12dp</item>
        <item name="android:paddingRight">12dp</item>
        <item name="android:minWidth">0dp</item>
    </style>

    <style name="ToolbarStyle" parent="Widget.AppCompat.Toolbar">
        <item name="titleTextAppearance">@style/ToolbarTitleTextAppearance</item>
        <item name="android:gravity">center_vertical</item>
        <!--<item name="android:background">@drawable/actionbar_bg</item>-->
    </style>

    <style name="ActionMenuTextAppearance" parent="TextAppearance.AppCompat.Widget.ActionBar.Menu">
        <!--<item name="android:textColor">@color/primary_green</item>
        <item name="android:textSize">@dimen/font_size_14</item>-->
    </style>

    <style name="AppTheme" parent="BaseTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowBackground">@color/bg_color</item>
        <item name="actionBarSize">@dimen/action_bar_default_size</item>
        <item name="toolbarStyle">@style/ToolbarStyle</item>
        <item name="toolbarNavigationButtonStyle">@style/ToolbarNavigationButtonStyle</item>


        <!--toolbar和状态栏相关！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！ -->
        <!-- tool_bar（actionbar）颜色 设置背景时，需要去掉此项以及布局中的android:background="?attr/colorPrimary"-->
        <!--<item name="colorPrimary">@color/primary_green</item>-->
        <!-- 状态栏颜色 (当状态栏透明时)-->
        <!--<item name="colorPrimaryDark">@color/black_light</item>

        <item name="android:windowBackground">@color/bg_color</item>

        <item name="actionBarSize">50dp</item>

        <item name="toolbarStyle">@style/ToolbarStyle</item>
        <item name="toolbarNavigationButtonStyle">@style/ToolbarNavigationButtonStyle</item>
        <item name="actionMenuTextAppearance">@style/ActionMenuTextAppearance</item>
        <item name="actionMenuTextColor">@color/primary_green</item>-->
    </style>

    <style name="SplashTheme" parent="AppTheme">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/background</item>
    </style>

</resources>
